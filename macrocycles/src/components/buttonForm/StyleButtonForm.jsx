import { StyleSheet } from 'react-native'
import theme from '../../theme/theme'

const StyleButtonForm = StyleSheet.create({
  buttonForm: {
    paddingVertical: 5,
    borderRadius: 5,
    textAlign: 'center',
    color: theme.colors.white
  }
})

export default StyleButtonForm
