import { StyleSheet } from 'react-native'
import theme from '../../theme/theme'

const StyleLinkForm = StyleSheet.create({
  link: {
    color: theme.colors.green.default,
    borderBottomWidth: 1,
    borderColor: theme.colors.green.default
  }
})

export default StyleLinkForm
