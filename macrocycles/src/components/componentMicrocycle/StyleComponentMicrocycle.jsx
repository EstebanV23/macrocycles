import { StyleSheet } from 'react-native'

const StyleComponentMicrocycle = StyleSheet.create({
  content: {
    display: 'flex',
    flexDirection: 'column',
    alignItems: 'center',
    marginHorizontal: 3
  }
})

export default StyleComponentMicrocycle
