import { StyleSheet } from 'react-native'

const StyleOnlyMicroData = StyleSheet.create({
  contentInputs: {
    display: 'flex',
    flexDirection: 'row',
    justifyContent: 'space-between',
    gap: 10
  }
})

export default StyleOnlyMicroData
