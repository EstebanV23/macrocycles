import { StyleSheet } from 'react-native'
import theme from '../../theme/theme'

const StyleLinkNavbar = StyleSheet.create({
  link: {
    color: theme.colors.blue.default,
    borderRadius: '50%',
    padding: 3
  }
})

export default StyleLinkNavbar
