export default async function serviceAllDataMacro (data) {
  if (!data) throw new Error('No se ha definido el roadMap')
  console.log('🚀 ~ file: serviceAllDataMacro.js:2 ~ serviceAllDataMacro ~ data:', data)
  return (
    <div>serviceAllDataMacro</div>
  )
}
